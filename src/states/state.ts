/* eslint-disable prefer-const */

import { ref, reactive, computed } from 'vue';
import type { Ref } from 'vue';

/**************************************************/

/**************************************************/

/**************************************************/

export const quantitize: Ref<number> = ref(120); // 1 <= quantitize
